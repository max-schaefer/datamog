# Exercise 8.1 — Leaf nodes.

input predicate edge(src: string, dst: string).

# Placeholder.
leaf(X) :- edge(X, _), X = "__never_matches__".

# TODO: an intermediate "has outgoing" predicate, and a leaf rule
# that negates it.

?- leaf(X).
