# Exercise 8.3 — Set difference and intersection.

input predicate a(x: string).
input predicate b(x: string).

# Placeholders.
only_in_a(X) :- a(X), X = "__never_matches__".
output predicate only_in_b(X) :- b(X), X = "__never_matches__".
output predicate in_both(X) :- a(X), X = "__never_matches__".

# TODO: float rules.

?- only_in_a(X).
