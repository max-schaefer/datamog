# Exercise 9.2 — Ranks via filtering.

input predicate scores(student: string, subject: string, score: integer).

# TODO: write avg_by_subject and top_subject.

# Placeholders.
avg_by_subject(S, avg(Score)) :- scores(_, S, Score), S = "__never_matches__".
output predicate top_subject(S) :- avg_by_subject(S, _).

?- avg_by_subject(S, A).
