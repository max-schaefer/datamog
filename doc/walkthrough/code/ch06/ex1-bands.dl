# Exercise 6.1 — Grading bands.

input predicate score(student: string, subject: string, points: integer).

# Placeholders so all four queries parse; replace with real rules.
grade_a(S, Sub) :- score(S, Sub, P), P = -1.
output predicate grade_b(S, Sub) :- score(S, Sub, P), P = -1.
output predicate grade_c(S, Sub) :- score(S, Sub, P), P = -1.
output predicate grade_f(S, Sub) :- score(S, Sub, P), P = -1.

# TODO: replace each placeholder with the correct band rule.

?- grade_a(S, Sub).
