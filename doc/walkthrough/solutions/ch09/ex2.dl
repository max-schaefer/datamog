# Exercise 9.2 solution.

extensional scores(student: string, subject: string, score: integer).

avg_by_subject(Subject, avg(Score)) :- scores(_, Subject, Score).

output predicate top_subject(Subject) :- avg_by_subject(Subject, A), A > 83.

?- avg_by_subject(S, A).
