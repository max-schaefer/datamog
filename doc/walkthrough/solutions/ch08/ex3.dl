# Exercise 8.3 solution.

extensional a(x: string).
extensional b(x: string).

only_in_a(X) :- a(X), not b(X).
output predicate only_in_b(X) :- b(X), not a(X).
output predicate in_both(X)   :- a(X), b(X).

?- only_in_a(X).
