This example uses non-linear recursion (deriving a CNF needs the clause
sets of both children of a binary connective), which the SQL backends
reject. The examples test therefore skips the sqlite backend here and uses
the seminaive backend as the canonical source for expected.json.
